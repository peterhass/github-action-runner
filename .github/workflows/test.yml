name: Test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  smoke:
    name: ${{ matrix.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Debian stable
            image: debian:stable
            install: apt-get update && apt-get install --yes bash coreutils grep make shellcheck
          - name: Arch Linux
            image: archlinux:latest
            install: pacman -Syu --noconfirm --needed bash coreutils grep make shellcheck
    container:
      image: ${{ matrix.image }}
    steps:
      - name: Install test dependencies
        run: ${{ matrix.install }}
      - uses: actions/checkout@v4
      - name: Run smoke tests
        run: make test
